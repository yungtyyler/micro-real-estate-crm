import React from "react";

const Footer = () => {
  return (
    <footer className="bg-gray-100 text-center py-3 text-sm">
      © {new Date().getFullYear()} The Varzeas Group
    </footer>
  );
};

export default Footer;
